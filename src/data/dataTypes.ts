import { type TableNames } from '../../convex/_generated/dataModel';

export const DataTypes: TableNames[] = [
  'buildings',
  'components',
  'crossSections',
  'geometries',
  'rebars',
  'users',
  'materials'
] as const;
