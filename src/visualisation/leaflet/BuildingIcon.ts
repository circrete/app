import { icon } from 'leaflet';
import circreteLogo from '../../assets/circrete-logo.avif';

export const CircreteIcon = icon({
  iconUrl: circreteLogo,
  iconSize: [32, 32],
  iconAnchor: [16, 16]
});
